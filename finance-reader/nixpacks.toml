[providers]
providers = ["node", "python"]

[phases.setup]
nixPkgs = ["nodejs-18_x", "python310", "gcc"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo Build completed"]

[start]
cmd = "node server.js"