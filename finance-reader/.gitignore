# ---- 환경변수 / 시크릿 (절대 커밋 금지) ----
.env
.env.local
.env.*.local

# ---- Node.js ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---- Python ----
__pycache__/
*.pyc
.venv/
venv/
env/
*.egg-info/

# ---- OS / 에디터 ----
.DS_Store
Thumbs.db
.vscode/
.idea/

# ---- 대용량/중간 산출물 (재생성 가능한 파일) ----
# DART 원본 체크포인트는 중간 저장용이라 커밋 불필요 (최종본만 필요시 커밋)
dart_financial_data_checkpoint.csv
feature_importance.png
threshold_analysis.csv

# ---- 로그 ----
*.log

# ---- 런타임에 쌓이는 사용자 데이터 / 캐시 (개인정보 포함 가능성 있음 — 절대 커밋 금지) ----
data/
docs_cache/

# ---- 대용량 모델 및 데이터셋 (GitHub 업로드 방지) ----
#*.pkl
#*.csv
naver-debug.html